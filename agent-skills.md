# skills

## general (opencode)

install to `~/.agent/skills`, symlink

> python & rust

```shell
pnpx skills add PsiACE/skills --skill='*' -g -y
```

> vue

```shell
pnpx skills add antfu/skills --skill='*' -g -y
```

> ui & ux

```shell
pnpx skills add https://github.com/nextlevelbuilder/ui-ux-pro-max-skill --skill ui-ux-pro-max -g -y
```

> dev

```shell
pnpx skills add luoling8192/software-design-philosophy-skill -g -y
```

> ios

```shell
pnpx skills add https://github.com/wshobson/agents --skill mobile-ios-design -g -y
pnpx skills add https://github.com/avdlee/swiftui-agent-skill --skill swiftui-expert-skill -g -y
```

> client

```shell
pnpx skills add yetone/native-feel-skill -g -y
```

> database

```shell
pnpx skills add https://github.com/supabase/agent-skills --skill supabase-postgres-best-practices -g -y
```

> context engineering

```shell
pnpx skills add OthmanAdi/planning-with-files --skill planning-with-files -g -y
```

> obsidian

```shell
pnpx skills add https://github.com/kepano/obsidian-skills -g -y
```
